<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhaojy.planttudian">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ui.application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.LoginActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.SearchActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.WanTuActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.PlantActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.WanTuDetailActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.ArticleActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.ArticleDetailActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.ArticleClassifyActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.FindMoreActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activity.SetActivity"
            android:launchMode="singleTask" />

        <activity android:name="com.zhaojy.selectlibrary.view.PhotoSelectActivity" />

    </application>

</manifest>